package phone.number.cheker.demo;

/**
 * this enum contains states(status) of telephone number, depending on it`s existence in DB tables
 */

public enum TelNumStatus {
    ACCEPT,CHALLENGE,DECLINE;

}
